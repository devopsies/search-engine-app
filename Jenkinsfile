pipeline {
    agent any

    stages {
        stage ('Generate the test cases') {
            steps {
                sh( returnStdout: false, script: """#!/bin/sh
                    echo gg
                    """.stripIndent()
                )
                sh "echo gg"
                )
            }
        }
    }
}
